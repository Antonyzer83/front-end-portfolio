export class Companies {
    constructor(
        company_id: number,
        name: string,
        loc: string,
        history: string,
        activity: string,
        context: string,
        img_loc: string
    ) {
    }
}