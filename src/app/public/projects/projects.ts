export class Projects {
    constructor(
        project_id: number,
        title: string,
        from_date: Date,
        to_date: Date,
        intro: string,
        goal: string,
        experience: string,
        git: string,
        img_id: number,
        img_loc: string
    ) {
    }
}