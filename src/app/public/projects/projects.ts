export class Projects {
    constructor(
        project_id: Int16Array,
        title: string,
        from_date: Date,
        to_date: Date,
        intro: string,
        goal: string,
        experience: string,
        git: string,
        img_id: Int16Array
    ) {
    }
}