export class Experiences {
    constructor(
        experience_id: Int16Array,
        company: string,
        workplace: string,
        from_date: Date,
        to_date: Date,
        descr: string,
        mission: string,
        link: string
    ) { }
}