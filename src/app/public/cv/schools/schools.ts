export class Schools {
    constructor(
        study_id: Int16Array,
        school: string,
        diplom: string,
        from_date: Date,
        to_date: Date,
        descr: string
    ) {
    }
}